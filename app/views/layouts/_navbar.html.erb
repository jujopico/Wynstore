<nav>
  <%= link_to "Home", root_url, class: "navbar-brand" %>

  <div>
      <div>
        <%= link_to "Items", items_url %>
      </div>
      <% if current_user %>
      <div>
        <%= link_to "My Cart (#{current_user.carts.last.items.count})", cart_path(current_user.carts.last) %>
      </div>
      <% end %>
  </div>
</nav>
