<nav>
  <%= link_to "Home", root_url, class: "navbar-brand" %>

  <div>
      <div>
        <%= link_to "Items", items_url %>
      </div>
      <% if current_or_guest_user %>
      <div>
        <%= link_to "My Cart (#{current_or_guest_user.carts.last.items.count})", cart_path(current_or_guest_user.carts.last) %>
      </div>
      <% end %>
  </div>
</nav>
